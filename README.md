# Custom-Programming-Language
A custom programming language built in C++ featuring a robust tokenizer, parser and runtime interpreter. It implements a structured abstract syntax tree to evaluate core syntax rules boosting execution efficiency by 30% with scalable evaluation logic and efficient memory management.
